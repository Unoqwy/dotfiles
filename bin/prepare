#!/bin/bash
base=$(readlink -f "$(dirname $(readlink -f "$0"))/..")

"$base/ansible/scripts/generate_install"
"$base/ansible/scripts/generate_vars"

if [[ ! -f "$base/.env" ]]; then
    echo "Created new .env file"
    cp "$base/.env.example" "$base/.env"
fi
