#!/bin/bash
base=$(readlink -f "$(dirname $(readlink -f "$0"))/..")

exec "$base/ansible/scripts/generate_install"

if [[ ! -f "$base/.env" ]]; then
    echo "Created new .env file"
    cp "$base/.env.example" "$base/.env"
fi
