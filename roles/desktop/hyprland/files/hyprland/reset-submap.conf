# https://github.com/hyprwm/Hyprland/issues/1972
# Solution is ugly asf but no alternative yet.

bind = , Escape, submap, reset

bind = , A, submap, reset
bind = , B, submap, reset
bind = , C, submap, reset
bind = , D, submap, reset
bind = , E, submap, reset
bind = , F, submap, reset
bind = , G, submap, reset
bind = , H, submap, reset
bind = , I, submap, reset
bind = , J, submap, reset
bind = , K, submap, reset
bind = , L, submap, reset
bind = , M, submap, reset
bind = , N, submap, reset
bind = , O, submap, reset
bind = , P, submap, reset
bind = , Q, submap, reset
bind = , R, submap, reset
bind = , S, submap, reset
bind = , T, submap, reset
bind = , U, submap, reset
bind = , V, submap, reset
bind = , W, submap, reset
bind = , X, submap, reset
bind = , Y, submap, reset
bind = , Z, submap, reset
bind = , 1, submap, reset
bind = , 2, submap, reset
bind = , 3, submap, reset
bind = , 4, submap, reset
bind = , 5, submap, reset
bind = , 6, submap, reset
bind = , 7, submap, reset
bind = , 8, submap, reset
bind = , 9, submap, reset
bind = , 0, submap, reset


# all modifier combinations for all the characters above
bind = $modm                , A, submap, reset
bind = $modm ALT            , A, submap, reset
bind = $modm CTRL           , A, submap, reset
bind = $modm SHIFT          , A, submap, reset
bind = $modm ALT CTRL       , A, submap, reset
bind = $modm ALT SHIFT      , A, submap, reset
bind = $modm CTRL SHIFT     , A, submap, reset
bind = $modm ALT CTRL SHIFT , A, submap, reset


bind = $modm                , B, submap, reset
bind = $modm ALT            , B, submap, reset
bind = $modm CTRL           , B, submap, reset
bind = $modm SHIFT          , B, submap, reset
bind = $modm ALT CTRL       , B, submap, reset
bind = $modm ALT SHIFT      , B, submap, reset
bind = $modm CTRL SHIFT     , B, submap, reset
bind = $modm ALT CTRL SHIFT , B, submap, reset


bind = $modm                , C, submap, reset
bind = $modm ALT            , C, submap, reset
bind = $modm CTRL           , C, submap, reset
bind = $modm SHIFT          , C, submap, reset
bind = $modm ALT CTRL       , C, submap, reset
bind = $modm ALT SHIFT      , C, submap, reset
bind = $modm CTRL SHIFT     , C, submap, reset
bind = $modm ALT CTRL SHIFT , C, submap, reset


bind = $modm                , D, submap, reset
bind = $modm ALT            , D, submap, reset
bind = $modm CTRL           , D, submap, reset
bind = $modm SHIFT          , D, submap, reset
bind = $modm ALT CTRL       , D, submap, reset
bind = $modm ALT SHIFT      , D, submap, reset
bind = $modm CTRL SHIFT     , D, submap, reset
bind = $modm ALT CTRL SHIFT , D, submap, reset


bind = $modm                , E, submap, reset
bind = $modm ALT            , E, submap, reset
bind = $modm CTRL           , E, submap, reset
bind = $modm SHIFT          , E, submap, reset
bind = $modm ALT CTRL       , E, submap, reset
bind = $modm ALT SHIFT      , E, submap, reset
bind = $modm CTRL SHIFT     , E, submap, reset
bind = $modm ALT CTRL SHIFT , E, submap, reset


bind = $modm                , F, submap, reset
bind = $modm ALT            , F, submap, reset
bind = $modm CTRL           , F, submap, reset
bind = $modm SHIFT          , F, submap, reset
bind = $modm ALT CTRL       , F, submap, reset
bind = $modm ALT SHIFT      , F, submap, reset
bind = $modm CTRL SHIFT     , F, submap, reset
bind = $modm ALT CTRL SHIFT , F, submap, reset


bind = $modm                , G, submap, reset
bind = $modm ALT            , G, submap, reset
bind = $modm CTRL           , G, submap, reset
bind = $modm SHIFT          , G, submap, reset
bind = $modm ALT CTRL       , G, submap, reset
bind = $modm ALT SHIFT      , G, submap, reset
bind = $modm CTRL SHIFT     , G, submap, reset
bind = $modm ALT CTRL SHIFT , G, submap, reset


bind = $modm                , H, submap, reset
bind = $modm ALT            , H, submap, reset
bind = $modm CTRL           , H, submap, reset
bind = $modm SHIFT          , H, submap, reset
bind = $modm ALT CTRL       , H, submap, reset
bind = $modm ALT SHIFT      , H, submap, reset
bind = $modm CTRL SHIFT     , H, submap, reset
bind = $modm ALT CTRL SHIFT , H, submap, reset


bind = $modm                , I, submap, reset
bind = $modm ALT            , I, submap, reset
bind = $modm CTRL           , I, submap, reset
bind = $modm SHIFT          , I, submap, reset
bind = $modm ALT CTRL       , I, submap, reset
bind = $modm ALT SHIFT      , I, submap, reset
bind = $modm CTRL SHIFT     , I, submap, reset
bind = $modm ALT CTRL SHIFT , I, submap, reset


bind = $modm                , J, submap, reset
bind = $modm ALT            , J, submap, reset
bind = $modm CTRL           , J, submap, reset
bind = $modm SHIFT          , J, submap, reset
bind = $modm ALT CTRL       , J, submap, reset
bind = $modm ALT SHIFT      , J, submap, reset
bind = $modm CTRL SHIFT     , J, submap, reset
bind = $modm ALT CTRL SHIFT , J, submap, reset


bind = $modm                , K, submap, reset
bind = $modm ALT            , K, submap, reset
bind = $modm CTRL           , K, submap, reset
bind = $modm SHIFT          , K, submap, reset
bind = $modm ALT CTRL       , K, submap, reset
bind = $modm ALT SHIFT      , K, submap, reset
bind = $modm CTRL SHIFT     , K, submap, reset
bind = $modm ALT CTRL SHIFT , K, submap, reset


bind = $modm                , L, submap, reset
bind = $modm ALT            , L, submap, reset
bind = $modm CTRL           , L, submap, reset
bind = $modm SHIFT          , L, submap, reset
bind = $modm ALT CTRL       , L, submap, reset
bind = $modm ALT SHIFT      , L, submap, reset
bind = $modm CTRL SHIFT     , L, submap, reset
bind = $modm ALT CTRL SHIFT , L, submap, reset


bind = $modm                , M, submap, reset
bind = $modm ALT            , M, submap, reset
bind = $modm CTRL           , M, submap, reset
bind = $modm SHIFT          , M, submap, reset
bind = $modm ALT CTRL       , M, submap, reset
bind = $modm ALT SHIFT      , M, submap, reset
bind = $modm CTRL SHIFT     , M, submap, reset
bind = $modm ALT CTRL SHIFT , M, submap, reset


bind = $modm                , N, submap, reset
bind = $modm ALT            , N, submap, reset
bind = $modm CTRL           , N, submap, reset
bind = $modm SHIFT          , N, submap, reset
bind = $modm ALT CTRL       , N, submap, reset
bind = $modm ALT SHIFT      , N, submap, reset
bind = $modm CTRL SHIFT     , N, submap, reset
bind = $modm ALT CTRL SHIFT , N, submap, reset


bind = $modm                , O, submap, reset
bind = $modm ALT            , O, submap, reset
bind = $modm CTRL           , O, submap, reset
bind = $modm SHIFT          , O, submap, reset
bind = $modm ALT CTRL       , O, submap, reset
bind = $modm ALT SHIFT      , O, submap, reset
bind = $modm CTRL SHIFT     , O, submap, reset
bind = $modm ALT CTRL SHIFT , O, submap, reset


bind = $modm                , P, submap, reset
bind = $modm ALT            , P, submap, reset
bind = $modm CTRL           , P, submap, reset
bind = $modm SHIFT          , P, submap, reset
bind = $modm ALT CTRL       , P, submap, reset
bind = $modm ALT SHIFT      , P, submap, reset
bind = $modm CTRL SHIFT     , P, submap, reset
bind = $modm ALT CTRL SHIFT , P, submap, reset


bind = $modm                , Q, submap, reset
bind = $modm ALT            , Q, submap, reset
bind = $modm CTRL           , Q, submap, reset
bind = $modm SHIFT          , Q, submap, reset
bind = $modm ALT CTRL       , Q, submap, reset
bind = $modm ALT SHIFT      , Q, submap, reset
bind = $modm CTRL SHIFT     , Q, submap, reset
bind = $modm ALT CTRL SHIFT , Q, submap, reset


bind = $modm                , R, submap, reset
bind = $modm ALT            , R, submap, reset
bind = $modm CTRL           , R, submap, reset
bind = $modm SHIFT          , R, submap, reset
bind = $modm ALT CTRL       , R, submap, reset
bind = $modm ALT SHIFT      , R, submap, reset
bind = $modm CTRL SHIFT     , R, submap, reset
bind = $modm ALT CTRL SHIFT , R, submap, reset


bind = $modm                , S, submap, reset
bind = $modm ALT            , S, submap, reset
bind = $modm CTRL           , S, submap, reset
bind = $modm SHIFT          , S, submap, reset
bind = $modm ALT CTRL       , S, submap, reset
bind = $modm ALT SHIFT      , S, submap, reset
bind = $modm CTRL SHIFT     , S, submap, reset
bind = $modm ALT CTRL SHIFT , S, submap, reset


bind = $modm                , T, submap, reset
bind = $modm ALT            , T, submap, reset
bind = $modm CTRL           , T, submap, reset
bind = $modm SHIFT          , T, submap, reset
bind = $modm ALT CTRL       , T, submap, reset
bind = $modm ALT SHIFT      , T, submap, reset
bind = $modm CTRL SHIFT     , T, submap, reset
bind = $modm ALT CTRL SHIFT , T, submap, reset


bind = $modm                , U, submap, reset
bind = $modm ALT            , U, submap, reset
bind = $modm CTRL           , U, submap, reset
bind = $modm SHIFT          , U, submap, reset
bind = $modm ALT CTRL       , U, submap, reset
bind = $modm ALT SHIFT      , U, submap, reset
bind = $modm CTRL SHIFT     , U, submap, reset
bind = $modm ALT CTRL SHIFT , U, submap, reset


bind = $modm                , V, submap, reset
bind = $modm ALT            , V, submap, reset
bind = $modm CTRL           , V, submap, reset
bind = $modm SHIFT          , V, submap, reset
bind = $modm ALT CTRL       , V, submap, reset
bind = $modm ALT SHIFT      , V, submap, reset
bind = $modm CTRL SHIFT     , V, submap, reset
bind = $modm ALT CTRL SHIFT , V, submap, reset


bind = $modm                , W, submap, reset
bind = $modm ALT            , W, submap, reset
bind = $modm CTRL           , W, submap, reset
bind = $modm SHIFT          , W, submap, reset
bind = $modm ALT CTRL       , W, submap, reset
bind = $modm ALT SHIFT      , W, submap, reset
bind = $modm CTRL SHIFT     , W, submap, reset
bind = $modm ALT CTRL SHIFT , W, submap, reset


bind = $modm                , X, submap, reset
bind = $modm ALT            , X, submap, reset
bind = $modm CTRL           , X, submap, reset
bind = $modm SHIFT          , X, submap, reset
bind = $modm ALT CTRL       , X, submap, reset
bind = $modm ALT SHIFT      , X, submap, reset
bind = $modm CTRL SHIFT     , X, submap, reset
bind = $modm ALT CTRL SHIFT , X, submap, reset


bind = $modm                , Y, submap, reset
bind = $modm ALT            , Y, submap, reset
bind = $modm CTRL           , Y, submap, reset
bind = $modm SHIFT          , Y, submap, reset
bind = $modm ALT CTRL       , Y, submap, reset
bind = $modm ALT SHIFT      , Y, submap, reset
bind = $modm CTRL SHIFT     , Y, submap, reset
bind = $modm ALT CTRL SHIFT , Y, submap, reset


bind = $modm                , Z, submap, reset
bind = $modm ALT            , Z, submap, reset
bind = $modm CTRL           , Z, submap, reset
bind = $modm SHIFT          , Z, submap, reset
bind = $modm ALT CTRL       , Z, submap, reset
bind = $modm ALT SHIFT      , Z, submap, reset
bind = $modm CTRL SHIFT     , Z, submap, reset
bind = $modm ALT CTRL SHIFT , Z, submap, reset


bind = $modm                , 1, submap, reset
bind = $modm ALT            , 1, submap, reset
bind = $modm CTRL           , 1, submap, reset
bind = $modm SHIFT          , 1, submap, reset
bind = $modm ALT CTRL       , 1, submap, reset
bind = $modm ALT SHIFT      , 1, submap, reset
bind = $modm CTRL SHIFT     , 1, submap, reset
bind = $modm ALT CTRL SHIFT , 1, submap, reset


bind = $modm                , 2, submap, reset
bind = $modm ALT            , 2, submap, reset
bind = $modm CTRL           , 2, submap, reset
bind = $modm SHIFT          , 2, submap, reset
bind = $modm ALT CTRL       , 2, submap, reset
bind = $modm ALT SHIFT      , 2, submap, reset
bind = $modm CTRL SHIFT     , 2, submap, reset
bind = $modm ALT CTRL SHIFT , 2, submap, reset


bind = $modm                , 3, submap, reset
bind = $modm ALT            , 3, submap, reset
bind = $modm CTRL           , 3, submap, reset
bind = $modm SHIFT          , 3, submap, reset
bind = $modm ALT CTRL       , 3, submap, reset
bind = $modm ALT SHIFT      , 3, submap, reset
bind = $modm CTRL SHIFT     , 3, submap, reset
bind = $modm ALT CTRL SHIFT , 3, submap, reset


bind = $modm                , 4, submap, reset
bind = $modm ALT            , 4, submap, reset
bind = $modm CTRL           , 4, submap, reset
bind = $modm SHIFT          , 4, submap, reset
bind = $modm ALT CTRL       , 4, submap, reset
bind = $modm ALT SHIFT      , 4, submap, reset
bind = $modm CTRL SHIFT     , 4, submap, reset
bind = $modm ALT CTRL SHIFT , 4, submap, reset


bind = $modm                , 5, submap, reset
bind = $modm ALT            , 5, submap, reset
bind = $modm CTRL           , 5, submap, reset
bind = $modm SHIFT          , 5, submap, reset
bind = $modm ALT CTRL       , 5, submap, reset
bind = $modm ALT SHIFT      , 5, submap, reset
bind = $modm CTRL SHIFT     , 5, submap, reset
bind = $modm ALT CTRL SHIFT , 5, submap, reset


bind = $modm                , 6, submap, reset
bind = $modm ALT            , 6, submap, reset
bind = $modm CTRL           , 6, submap, reset
bind = $modm SHIFT          , 6, submap, reset
bind = $modm ALT CTRL       , 6, submap, reset
bind = $modm ALT SHIFT      , 6, submap, reset
bind = $modm CTRL SHIFT     , 6, submap, reset
bind = $modm ALT CTRL SHIFT , 6, submap, reset


bind = $modm                , 7, submap, reset
bind = $modm ALT            , 7, submap, reset
bind = $modm CTRL           , 7, submap, reset
bind = $modm SHIFT          , 7, submap, reset
bind = $modm ALT CTRL       , 7, submap, reset
bind = $modm ALT SHIFT      , 7, submap, reset
bind = $modm CTRL SHIFT     , 7, submap, reset
bind = $modm ALT CTRL SHIFT , 7, submap, reset


bind = $modm                , 8, submap, reset
bind = $modm ALT            , 8, submap, reset
bind = $modm CTRL           , 8, submap, reset
bind = $modm SHIFT          , 8, submap, reset
bind = $modm ALT CTRL       , 8, submap, reset
bind = $modm ALT SHIFT      , 8, submap, reset
bind = $modm CTRL SHIFT     , 8, submap, reset
bind = $modm ALT CTRL SHIFT , 8, submap, reset


bind = $modm                , 9, submap, reset
bind = $modm ALT            , 9, submap, reset
bind = $modm CTRL           , 9, submap, reset
bind = $modm SHIFT          , 9, submap, reset
bind = $modm ALT CTRL       , 9, submap, reset
bind = $modm ALT SHIFT      , 9, submap, reset
bind = $modm CTRL SHIFT     , 9, submap, reset
bind = $modm ALT CTRL SHIFT , 9, submap, reset


bind = $modm                , 0, submap, reset
bind = $modm ALT            , 0, submap, reset
bind = $modm CTRL           , 0, submap, reset
bind = $modm SHIFT          , 0, submap, reset
bind = $modm ALT CTRL       , 0, submap, reset
bind = $modm ALT SHIFT      , 0, submap, reset
bind = $modm CTRL SHIFT     , 0, submap, reset
bind = $modm ALT CTRL SHIFT , 0, submap, reset
